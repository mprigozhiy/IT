README Server/Client Program

This is a server/client program that allows users (clients) to POST messages to a central server and GET messages from the server. Messages will be stored/grouped together according to a group name specified by the POST client user. GET clients can request all messages from a specified group name.